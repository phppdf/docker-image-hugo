services:
  docs-webserver:
    build:
      context: .
      dockerfile: src/Dockerfile
    image: ghcr.io/phppdf/docker-image-hugo:dev
    container_name: phppdf-docker-image-hugo-docs
    volumes:
      - ./:/src
    ports:
      - "1313:1313"
